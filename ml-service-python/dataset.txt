arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>sum_val=0<nl>for x indexn arr:<nl>    sum_val+=x<nl>prindexnt(sum_val)||2
def fib(n):<nl>    if n<=1:<nl>        return n<nl>    return fib(n-1)+fib(n-2)||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
n=indexnt(indexnput())<nl>resum_val=0<nl>for index indexn range(n):<nl>    resum_val+=index<nl>prindexnt(resum_val)||2
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
n=int(input())<nl>res=0<nl>for i in range(n):<nl>    res+=i<nl>print(res)||0
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
def binary_search(arr,x):<nl>    l,r=0,len(arr)-1<nl>    while l<=r:<nl>        mid=(l+r)//2<nl>        if arr[mid]==x:<nl>            return mid<nl>        elif arr[mid]<x:<nl>            l=mid+1<nl>        else:<nl>            r=mid-1<nl>    return -1||1
arr=[1,2,3,4]<nl>s=0<nl>for x in arr:<nl>    s+=x<nl>print(s)||0
